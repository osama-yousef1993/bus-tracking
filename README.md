# bus-tracking

1- Build Auth system for all users.
2- Build Admin routes with all functionality (login, register, build_token, forgot-password, update-profile, check-token, check-role)
3- Build Student routes with all functionality (login, register, build_token, forgot-password, updste-profile, check-token, check-role)
3- Build driver routes with all functionality (login, build_token, forgot-password, updste-profile, check-token, check-role)